{
  "name": "php-dev",
  "description": "Grunt tasks for the demo",
  "license": "BSD",
  "devDependencies": {
    "grunt": "0.4.x",
    "grunt-contrib-uglify": "0.2.x",
    "grunt-contrib-concat": "0.3.x",
    "grunt-s3": "0.2.x",
    "grunt-rev": "0.1.x",
    "grunt-usemin": "0.1.12"
  },
  "readmeFilename": "readme.markdown",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
